#!/usr/bin/env bash

# Placeholder theme picker (Ctrl+Shift+Super+Space)
# You can extend this to switch colors.conf and waybar styles, then reload.

set -euo pipefail

notify-send "Theme" "Theme picker not implemented yet. Configure colors.conf and reload." || true

