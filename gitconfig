[core]
  excludesfile = ~/dotfiles/gitignore
  quotepath = false

[commit]
  template = ~/dotfiles/gitmessage

[merge]
  tool = vimdiff

[alias]
  push-f = push --force-with-lease
  plog = log --no-merges --date=short --pretty='format:%C(yellow)%h %C(green)%cd %C(blue)%an%C(red)%d %C(reset)%s'

[http]
  postBuffer = 1048576000

[push]
  default = simple
